@echo off
rem ==============================================================================
rem NSEFO Master Pro — Windows Setup Alias
rem Single-word command: setup.bat  (same as: install.bat)
rem Runs the full Windows installer with pre-flight checks.
rem ==============================================================================
call "%~dp0install.bat" %*