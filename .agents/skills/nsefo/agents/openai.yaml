interface:
  display_name: "Nsefo"
  short_description: "Repo-specific patterns and workflows for nsefo"
  default_prompt: "Use the nsefo repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true